# DUMMY / local mode (for competition judges — no env needed, just this):
#   npm install
#   npm run db:setup
#   npm run dev
# Dummy logins: demo@impactquest.local/demo1234, admin@impactquest.local/admin1234, org@impactquest.local/org1234
DATABASE_PATH=data/impactquest.db
NEXT_PUBLIC_DEMO_MODE=true